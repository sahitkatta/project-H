# Set to `true` to prefill forms with sample values for local QA.
# Leave unset or `false` in real environments.
# See `src/dev/formTestPrefill.ts` for details and cleanup notes.
VITE_FORM_TEST_PREFILL=false
